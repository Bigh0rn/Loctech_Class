    function sum(num1, num2) {
           let add = num1 + num2
            console.log(add)
        }
sum(7, 3)

function fullName(firstName, lastName){
    let printFullName = firstName + ' ' + lastName
    return printFullName 
}
console.log(fullName('Deji', 'Rotifa'))

const areaofCircle = (radius) => Math.PI * radius * radius
console.log(areaofCircle(7).toFixed(5))
